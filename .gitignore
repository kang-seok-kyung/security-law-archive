# node_modules 폴더 무시 (frontend, backend 모두 포함)
**/node_modules/

# 환경 변수 파일 무시 (각 폴더의 .env 포함)
**/*.env

# 로그 파일 무시
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 빌드 결과물 무시 (React 기본 빌드 폴더)
frontend/build/
backend/build/

# MacOS 시스템 파일 무시 (필요시)
.DS_Store